services:
  mushroom_monitor:
    image: shayne651/mushroom_monitor:latest
    volumes:
      - mush_db:/src/mush.db
    ports:
      - 7891:7891
    environment:
      - MIGRATION_LOCATION=/db/migrations
volumes:
  mush_db: